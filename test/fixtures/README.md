Put a real Outlook Classic `.oft` fixture with at least one inline image here as:

outlook-inline-image.oft

The fixture test is skipped when the file is absent. When present, it checks the
MAPI properties Outlook writes for inline image attachments.
